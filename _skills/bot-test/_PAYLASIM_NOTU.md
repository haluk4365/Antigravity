# Paylasim Notu — bot-test

**Mod:** A (+ envanter sablonlandi)

## Ne yapildi
- `health_check.py` — `BOTS` listesi sahibin gercek Railway proje/servis/environment ID'lerini iceriyordu; tek jenerik ornek bot girdisine indirildi (`<RAILWAY_*_ID>` placeholder).

## Ogrenci ne yapmali
- `health_check.py` icindeki `BOTS` listesine kendi botlarinizi ekleyin: `token_env` (master.env'deki degisken adi) + Railway proje/servis/env ID'leri.
- `master.env` icine ilgili `TELEGRAM_*_BOT_TOKEN` ve `RAILWAY_TOKEN` degiskenlerini girin.
