# Paylaşım Notu — use-railway

**Mod:** A

**Ne yapıldı:**
- `SKILL.md`: monorepo repo adı `<GITHUB_REPO>`, örnek mutlak yol `<ANTIGRAVITY_ROOT>` placeholder'ına çevrildi.
- `scripts/railway-api.sh`: hardcoded `master.env` yolu script konumundan türetiliyor (`ANTIGRAVITY_ROOT` env override destekli).

**Öğrenci ne yapmalı:** `_knowledge/credentials/master.env`'e `RAILWAY_TOKEN` ekle.
